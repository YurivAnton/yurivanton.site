name: Deploy to Hosting
on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@master
        
      - name: copy files to server     
        uses: appleboy/scp-action@master
        with:
            host: yurivanton.site
            username: anton.yurivanton.site
            password: AntonYuriv1!
            port: 22
            source: "./*"
            target: "/yurivanton.site/web/yurivanton.site"
