name: Deployment Workflow
on:
  push:
    branches: [main]
jobs:
  job_one:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: testing server ssh connection
      uses: appleboy/ssh-action@master
      with:
        host: shell.r6.websupport.sk
        username: uid5485023
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: 29743
        sctipt: |
          mkdir ~/yurivanton.site/test
