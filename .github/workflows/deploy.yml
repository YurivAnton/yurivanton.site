name: 🚀 FTP Deploy to Websupport

on:
  push:
    branches:
      - main  # Заміни на свою гілку, якщо потрібно

jobs:
  ftp-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🛠 Install Composer dependencies (якщо потрібно)
        run: composer install --no-dev --prefer-dist --optimize-autoloader

      - name: 🧼 Cleanup (опційно)
        run: |
          rm -rf tests
          rm -rf .git
          rm -rf node_modules

      - name: 📤 FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          port: 21
             # Заміни на твій шлях
          local-dir: ./       # Завантажує всі локальні файли
          exclude: |
            **/.git*
            **/.github*
            **/node_modules/**
            **/tests/**
            .env
            README.md
